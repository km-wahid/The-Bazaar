
from django.urls import path
from product import views

# urlpatterns = [
#  path("<int:pk>/", views.ViewSpecificProduct.as_view(), name = "specfic_products"),   
#  path("", views.ProductList.as_view(), name = "products-list"),   
# ]